"""V1.0 business rubric, with the user's continuous-interview exceptions."""

INTERVIEW = '''
判断的是有限资金、人力与老板精力是否值得投入，不是替老板证明项目能成。
按逐项检查清单深入，不限制访谈总轮数，不为少问几轮而跳过关键事实。每轮仍问1至2个聚焦问题，问清楚后再收口：战略看最终经营指标及当前主业；市场看可触达客户、需求频次、竞品成熟度与差异化；资源看实际能力证明而非自信。
在讨论未来试点目标前，先核实本项目有没有已经完成的测试或实际运行，以及可参考的类似项目实绩。不能把用户没主动提到、没有上传附件、未来计划或目标值当成“没有测试”。已有结果就逐步问清期间、样本范围、基线与实际效果、完整成本和准确性、记录来源与核对人；类似案例还要问迁移条件和差异。已明确没测过就保留未验证，不反复索要尚不存在的结果；记录遗失或记不清也不抹掉用户已陈述的测试经历。口述、上传和独立核验分别记录，不自动提高证据等级。
商业项目的回报与成本问题须包含适用的售价、毛利/单笔贡献、获客、转化、复购、物流交付、佣金、退货损耗、人工管理及固定费用；由这些输入检验回本、盈亏平衡与目标利润。无法提供的金额/数据明确未知，不伪算。不要一口气索要全部指标，按最可能改变判断的缺口每轮问1至2项。
内部项目用任务量、净节省工时、错误减少、审核返工维护与可兑现产能替代对外交易指标。复制同时看普通员工/SOP能否接手及客户、数据、内容、组织等资产，不能只说可推广。
现金看总占用、库存/设备、付款回款、最大损失与退出成本；机会成本同时比较替代方案、老板工时、核心员工和现有主业受影响程度。
同一次分析内用反方视角检验：最可能失败原因、竞争对手如何胜出、谨慎投资人为何拒绝、内部董事为何反对、能做是否等于该做、高估前提和低估成本。发现影响当前决策且用户能回答的矛盾，直接作为本轮问题。不要调用或宣称进入独立审查，不为反方另开一轮，也不提前生成评分。
'''

REPORT = '''
报告按V1.0标准说明为何值得占用有限资源。先在本次分析内检验最可能失败的前提、竞争优势、投资回报、主业资源冲突和被低估的成本，再形成分数与建议；不新增独立调用。cons至少3条最强且互不重复的具体反对理由，分别覆盖适用的竞争、投资与内部资源视角，标明事实/推断/待验证，并与相应维度理由、关键假设或限制一致；pros至少3条具体有依据的支持理由，不把同一优点换词凑数。
逐维使用正式标准的评分锚点，不把所有基本可行项目机械评为3或3.5，也不因希望得到某等级而加分：
- 战略：3有价值但非当前核心战略；4高度符合战略且与主业明显协同；5直接服务核心战略、明显强化未来核心能力且为当前重要方向。
- 市场/需求：商业项目3稳定需求且竞争充分；4市场较大增长且机会清晰；5大市场、增长、结构性窗口、强需求与竞争机会都有依据。内部提效不套外部市场规模，按内部任务量、需求频次、主业影响及相对替代方案的实际价值判断相应强弱，不能仅因没有外部客户压低分。
- 回报：3模型能算清但部分关键数字仍为预测；4模型基本跑通、关键数字有本项目数据；5完整单位经济或内部价值模型反复验证、结构清晰且回报确定性较高。产能价值与实际现金分别核算。
- 资源：3约六至七成能力具备、剩余可补；4大部分核心资源已具备且有较强证据；5已有结果证明明显能力优势。评价本次范围，额外扩张未知单列。
- 复制/资产：3部分能复制、部分依赖关键人；4复制路径明确、资产沉淀较强；5高度可复制且持续沉淀多种资产。不能只数店铺，要看普通员工接手、标准流程与可复用成果。
- 现金：3有占用但可承受；4现金健康且占用可控；5轻资产、回款或节省兑现快、资金占用低、失败退出成本低。
- 风险：3若干重要风险需持续管理；4风险已有明确解决方案；5主要风险低且已有成熟控制方法，不要求证明未来绝无风险。
- 机会成本：3与替代项目回报接近；4明显优于大部分可行备选；5在公司当前实际备选项目中基本为最优配置。不要求穷尽所有想象中的方案。
3/4/5之间可按证据支持程度给0.5分步长；理由须说明对应哪一档及剩余限制。证据等级上限与业务原始分分别计算；下列缺资料/低分规则仍适用。
重复成功可以由同一项目的多个周期或已覆盖样本证明，不强制另做新项目、新店或新商业模式；未来延续预测尚未发生，不抹掉已有多周期实绩。扣分所引用的缺口须说明它如何影响本次申请范围，不能把未申请扩张的未知当成本次已验证能力的反证。
5分是对应业务锚点充分成立，不是绝对完美或未来必定成功：战略不要求公司唯一战略；内部需求不要求外部大市场；多周期回报不要求未来预测提前变成实绩；资源与复制不要求所有未知人员或格式均已验证；现金不要求零支出；机会成本不要求未来不会出现更好方案。满足哪一档就给哪一档，若低于已满足的锚点，须指出本次范围内实际尚未满足的条件，不能另设上述更高门槛。
review_complete表示本次分析已检查反方理由及其对当前决策的影响，不表示另一次独立审查。已识别且有成熟控制的残余风险不自动等于审查未完成；若仍有未处理的重大矛盾或关键限制则填false，并在cons或关键未知中具体说明。
每个dimensions条目增加missing_evidence字符串：写明该维判断缺少的具体依据，或说明现有依据的局限；未知不补造。用户已明确说明的费用构成、维护安排或计算口径，不得再写“尚未明确”；尚无实测或凭证时，应写“安排已说明，缺少实际执行记录”。逐项核对missing_evidence与本报告assumptions及最新用户说明，不能一处写已明确、另一处写未知。reason须说明为何对应原始分，而非只复述项目。
原始分低于3时，增加negative_fact字段，逐字引用用户原话、公司资料或已核验证据中的明确不利事实。仅有“其他方案可能更好”“缺少对比”“尚未验证”不构成不利事实，不能标fact扣低分；无法作方向判断就用null，不调高分数凑等级。引用须包含决定性事实，不截掉假设、可能或未确认等限定。
proposal增加decision_brief对象，所有值为字符串：
definition=项目一句话定义；goal_and_success=最终经营目标和成功标准；biggest_risk=最大的单一风险；key_unknown=最关键未知与最缺证据；maximum_loss=区分用户可承受损失上限和实际最坏损失估计，不能把预算等同损失；assets=成功后可沉淀和复制的资产；allocation=建议资源、最少人员、预算和周期，用户未确认的数值明确为建议；upgrade_a=达到A仍需要的本项目证据和条件；upgrade_s=总分至少90、E3证据、战略/市场与需求/回报/资源/复制这五维原始分均至少4，以及重复验证、资源与组合可行、审查完成的门槛；stop=降级、停止和止损条件。
这些内容在暂缓评级时也保留已有判断；暂缓不能批准执行，allocation只给补证/调整任务。C或已知致命风险不能建议继续原高风险方案；只有实质条件改变后才可重新评估。未知金额写未知及取得依据的方法，不为填满字段杜撰。
对所有原始分、资金计算和建议做一致性核对：原始分/5×权重，单位统一；ROI是指定收入/成本口径的比值，不等于净利润率；内部释放工时不能直接计为现金节省。最终等级由程序按证据封顶和红线算，不能自行承诺达某个经营指标就一定升级A或S。
用户明确纠正指标口径后，全篇（包括停止条件、支持理由和验证任务）统一用新口径：广告ROAS=扣退款取消后的实收销售额/广告费，不能再简称ROI；投资ROI=(收入-全部投入成本)/全部投入成本。ROAS达标不等于盈利。给出净贡献或净利润公式时，必须逐项包含用户已列出的适用成本，尤其不能遗漏商品/代工采购和包材；还要包含物流税费、平台支付、达人佣金、履约、退货损耗、广告和客服等对应层级成本，避免同一成本重复扣除。已扣退款的收入不再重复扣退款额，但退货物流及不可回收货损仍是成本。固定费与内部工时分开说明。金额未知只给完整计算口径，不给虚构结果。若成本边界无法确认，明确公式仍待补齐，不写一个漏项公式冒充净利润。
引用任何用户预算、成本、现金或损失金额时，逐个与最新用户原文和结构化输入核对，不能将自己建议的数字改写成用户现有预算。已有金额若不完整，只说明缺少构成；需要试点预算建议时标明“建议、待确认”及推导，缺少成本依据时不凭空设一个数字。后续明确更正优先于旧值。
通过/停止条件同样区分来源：用户已明确的阈值沿用；模型新增的阈值在每次出现时标明“建议、待确认”，不能混入用户已确认条件或改写其金额、周期与累计/单次口径。无需为每个假设另造数值；无依据的阈值写需确认的具体业务条件。
最终核对引用中的否定和可逆性：不可退/不可撤销的承诺不得写成可退/可撤销，“不追加”不得漏写“不”。预算上限、历史沉没费用和新增最坏损失分开，不因正负语义翻转改变行动。
'''


def validate_rubric_reasons(proposal, project):
    """Reject explicit extra requirements; never replace the model's score."""
    import re
    for key, dim in proposal.get('dimensions', {}).items():
        score = dim.get('score')
        if score is None or score >= 5:
            continue
        reason = dim.get('reason', '')
        invalid = bool(key == 'opportunity' and re.search(r'未穷尽|没有穷尽|未覆盖所有备选', reason))
        invalid |= bool(key == 'risk' and re.search(r'低到无需管理|必须零风险|必须绝无风险', reason))
        invalid |= bool(key == 'market' and project.get('project_type') == 'internal'
                        and re.search(r'非外部大市场|没有外部客户.*(?:故|因此|未达)', reason))
        invalid |= bool(key == 'market' and project.get('project_type') == 'growth'
                        and re.search(r'内需型内部|(?:不适用|无需评估)外部市场规模', reason))
        if invalid:
            raise ValueError(f'{key}评分理由使用了正式标准没有的额外门槛：{reason}。'
                             '重新按该维原有锚点判断；可以保留原分但必须有本次范围内的真实依据，不得自动调高或迎合目标等级。')


def ground_low_scores(proposal, project, company, evidence, messages):
    """A model's low business score needs a source-backed negative, not just its fact label."""
    import json
    import re
    sources = [project.get('description', ''), json.dumps(company, ensure_ascii=False)]
    sources += [m.get('content', '') for m in project.get('messages', []) + messages if m.get('role') == 'user']
    sources += [e.get('content', '') for e in evidence if e.get('verification_status') == 'verified']
    for dim in proposal.get('dimensions', {}).values():
        score = dim.get('score')
        if score is None or score >= 3:
            continue
        quote = (dim.get('negative_fact') or '').strip()
        uncertain = re.search(r'可能|也许|未验证|尚未|未知|缺少|待确认|不一定|没证明', quote)
        if len(quote) < 6 or uncertain or not any(quote in s for s in sources):
            dim.update(score=None, basis='unknown', missing_evidence=(
                '低分所需的明确不利事实尚无可对应原文的依据；不能仅因缺资料判为低分。'
                + dim.get('missing_evidence', '')))
