# Zeabur 账号纠正与进度迁移

已通过用户指定的内置浏览器，在显示成员 78 的 Zeabur 账号和已有 qy 服务器上部署。

- 项目：6aa1e8166c3d9581b715945e
- 服务：6aa1e8586c3d9581b7159478
- 网址：https://sabc-rating-qy.zeabur.app
- 私有仓库：zxc9802/sabc-rating-workbench，master，e52b80843e21ca55468c5141f33564c8c00cd7e4
- 新部署：6aa1e8c881898898b92bf855；运行中，域名 PROVISIONED。
- /data 挂载 sabc-data；14 项应用环境变量已保存，模型与上海采集配置保留，站点 origin 改为新域名。

原数据库通过 SQLite backup 导出并校验后，在内置浏览器目标文件管理和终端恢复。恢复前确认目标记录数为 0，另存空库备份。迁移时包含 6 项目、45 证据、53 抓取记录、6 选源计划、4 任务、1 公司版本、2 评级和 131 条审计记录。全部 records、audit 有序内容哈希与原快照一致，审计链通过。原部署未删除。

新站已通过真实浏览器登录、历史列表、创建 G01-2 R2、聊天、宿迁查询、上海采集及保存证据。采集任务 aeec0c188e6c43c2abd16772f3253e88，HTTP 200。匿名 bootstrap 返回 401。

质量验证未完成：R2 回答已提供验证记录要求和待确认的停止阈值；事实保存后后端为 v3，但页面仍显示 v2 和待核对提示。刷新后内置浏览器的该站标签控制超时，新标签也超时，Zeabur 标签仍可读取。尚不能区分浏览器控制问题和站点界面问题；保留为待核查，不计通过。继续报告流程、评分及其余冻结用例。

完整迁移证据：artifacts/account-migration/proof.json；恢复截图 restored-proof.png。快照与环境备份仅保留在忽略目录中。

后续复查：关闭失去响应的测试标签后，原 G01-2 R2 从列表恢复为 v3，成功生成报告 8b5083901c5c47bda5d90087be323671。再次刷新后同一项目与报告保留。该例四维评分85/95/95/90，整体验收仍未完成。G01-1 R1 出现无效选源计划，保留失败；G01-3 已启动并验证处理中刷新恢复。
