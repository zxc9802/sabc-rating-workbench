# 八维访谈与报告生成

独立对抗性审查机制已移除。普通访谈仍根据项目事实核对八维信息，主动追问矛盾、风险与缺失依据；不再追加独立审查模型请求，也不要求独立审查通过才展示生成入口。

有待回答的问题时继续显示问题，不能仅因八维已有描述就标记访谈完成或隐藏追问。八维信息完整且没有待回答问题时，同一次访谈调用整理完整判断，界面直接询问是否生成报告，显示“生成报告 / 继续补充”。点击生成后，程序采用本轮整理的项目事实，计算、保存报告并打开报告页面，不再调用模型，也不要求再点击核对。公司基线确认及证据核验状态不会因此自动改变。

判断绑定项目、用户回答、公司基线和证据的输入指纹；资料改变时须更新判断，不能使用过期内容生成报告。历史报告及历史审查数据保留，前端不再显示独立审查记录。

设置 SABC_FAL_API_KEY 后首选 fal GLM，失败重试一次，再使用原提供商的 Luna，最后 DeepSeek。原 SABC_API_KEY 继续供 Luna 使用。密钥仅通过部署环境变量配置。
