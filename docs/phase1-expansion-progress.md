# 五阶段 Goal v2：第一阶段执行记录

2026-09-09。本轮仍在第一阶段，后续部署和质量验收尚未执行。

## 已验证变更

- 新增阿克苏（新疆）地区生产总值图表采集器：`aksu/gdp:2025`。
- 官网 `https://www.aks.gov.cn/sjkf/index.html` 提供 `/xjcms/api/data/sczz.jsp`，按起止季度查询。
- 正式采集器真实返回2025年四个季度及三次产业指标；亿元单位来自官网图表说明。不能把累计季度相加，也不能将地区数据当成全疆数据。
- 官网2026年选项存在提交值为2025年的问题，采集器采用明确年份参数，拒绝返回年份不一致、重复季度、缺失指标和非有限数值。
- 地方自动入口31个，全量采集示例42项。原有渠道保留。
- 修改前122项后端测试通过；新增9项阿克苏测试后，相关37项测试通过，正式采集器真实调用通过。

## 候选检查与重试

- `artifacts/phase1/candidates.json`：本轮180个候选入口，含旧清单及官方公告/链接补充。它不是全国所有地市的完整目录。
- `round1-entry.jsonl`：180条首次检查，41个返回页面、91个HTTP错误或跳转、48个网络失败。返回页面不等于业务数据接通。
- `round1-follow.jsonl`：73次官方跳转或目录链接跟进，29个返回页面、39个HTTP错误、5个网络失败。
- 首轮每平台最多3次；本轮脚本不自动重试或自动跟随跳转。失败接口的底层重试必须计入，不使用生产collect默认四次重试进行探测。
- 黑龙江官方政务服务网指向 `https://data.zwfw.hlj.gov.cn:8881/oportal/index`。人口目录搜索成功，三个候选预览均为空，尚未接通。末轮拟通过浏览器确认目录并换查询或公开文件方法，不重复人口预览。
- 阿克苏返回数据后正式代码验证属于成功能力复核，并非失败重试。
- 中山目录脚本地址返回200但实际为nginx错误HTML；芜湖脚本403，均未新增到生产目录。
- 后续仍需逐项完成剩余省市的实际取数探索、末轮不同方法最多3次、最终失败台账及选源模型接入。不能将入口检查完成视为第一阶段完成。

## 环境

- 原有 `.venv` 为Windows版本，未删除或修改；本机使用 `.venv-mac`。
- `.venv-mac/`、`.env.*` 已加入忽略规则（保留 `.env.example` 可提交）。
- computer-use不能控制Codex自身窗口（工具明确拒绝）；现有Ego Lite可正常控制，用于公开平台浏览器复查，未尝试绕过工具限制。

## 黑龙江末轮结果

computer-use在Ego Lite打开官方目录，切换到“数据下载”，真实下载了《黑龙江省地表水环境质量月报(2024年11月).pdf》（1,890KB，9页，8页含可提取文字）。这只证明浏览器文件能力。

末轮使用不同于人口表格预览的公开PDF方法：浏览器检查、官方文件下载路由检查、匿名后端采集器验证，共3次。最终匿名后端请求虽然获得下载跳转，但响应不满足PDF文件头校验，未接通；不使用浏览器生成的临时签名冒充无人值守采集。候选代码已从生产目录撤下，仅保留 `artifacts/phase1/heilongjiang_candidate_not_integrated.py` 作为失败诊断材料，本轮停止继续尝试黑龙江。

最终结果在 `artifacts/phase1/heilongjiang-collector-verified.json`，浏览器下载能力与自动接口能力必须分开。当前生产新增仍只有阿克苏，地方入口31个。

最终本机后端回归：131项通过（含阿克苏新增校验）。第一阶段其余省市及选源模型接入继续待执行，第二至第五阶段尚未开始。

## 第一阶段本轮收敛（2026-09-09）

- 最终新增阿克苏、宿迁两项正式采集能力，地方自动入口32个，全量示例43项；保留原有实现。
- 宿迁：`suqian/search:商务统计` 每次重新读取官方公开列表，选择首篇匹配统计文章，保留完整统计正文、来源、标题期间及累计口径限制；正式采集器验证为2026年6月商务统计，正文明确1—6月累计。证据在 `artifacts/phase1/suqian-collector-verified.json`。
- 180个候选入口首轮检查后，对临时连接错误在首轮补充107次单次连接重试；73次已跟进官方跳转或目录。末轮对161个未保留为既有实现的候选，使用配置网络路径、HTTPS官方入口、官方目录或脚本发现方法，总计399次操作，各候选至多3次。黑龙江单独完成浏览器/PDF方法的3次末轮尝试。缺少可用业务记录或无法形成可重复采集器的条目本轮不接入。
- 逐项处置表：`docs/phase1-source-disposition.csv`。此表只覆盖固定候选，不能声称全国所有地市平台已穷尽；未取得省级入口的地区仍是能力缺口。网页、目录和脚本返回200不是接入成功。
- 独立选源模型：`sabc/planner.py`，通过`SABC_PLANNER_BASE_URL`、`SABC_PLANNER_MODEL`、`SABC_PLANNER_API_KEY`配置。动态提供已实现来源与地区清单，先计划、再取数、再分析，保存每轮计划及无需取数/失败原因。
- 实测OpenLux `glm-5.3-flash`：无需取数场景和已知GitHub仓库选源通过；阿克苏初轮有格式/请求失败，保留失败记录。兼容完整JSON代码围栏后，真实阿克苏查询及采集入库成功，证据ID `870b47ce1f264fa2bb0a72cf1e2860a3`，6.19秒。此少量探测不等于第五阶段质量验收通过。
- 最终本机后端145项测试通过。密钥仅写入被忽略、权限0600的本机`.env`，示例文件不含密钥。
- 下一步第二阶段服务器只读核实；第三至第五阶段未执行。
